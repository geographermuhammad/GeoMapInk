name: Deploy to FTP

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Deploy to FTP
      uses: SamKirkland/FTP-Deploy-Action@3.1.1
      with:
        ftp-server: ftp.geomapink.com
        ftp-username: u296449670.israr230
        ftp-password: Shah@230 # Replace with your actual FTP password
        local-dir: .  # Update this path according to your project structure
        remote-dir: /home/u296449670/domains/geomapink.com/public_html
